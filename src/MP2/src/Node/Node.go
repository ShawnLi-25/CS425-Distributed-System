package node

import (
	"MP2/src/node"
	"log"
	"net"
)

type Node struct {
	node.Sender
	node.Listener
	node.Introducer
	node.Updater
}

func NodeBehavior() {
	/**
	Check if this server is introducer (hard core to VM01)
	Execute different logic
	**/
}
	**/	
}
